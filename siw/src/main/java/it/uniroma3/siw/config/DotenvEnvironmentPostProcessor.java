package it.uniroma3.siw.config;

/**
 * Superato: la gestione del file .env e delle proprietà di configurazione è gestita
 * in modo standard dalla dependency 'springboot-dotenv' in pom.xml e da 'spring.config.import'.
 */
public class DotenvEnvironmentPostProcessor {
    // Classe dismessa in favore della dependency ufficiale
}
