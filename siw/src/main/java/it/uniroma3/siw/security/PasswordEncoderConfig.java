package it.uniroma3.siw.security;

/**
 * Nota didattica SIW:
 * Il bean PasswordEncoder (BCryptPasswordEncoder) è configurato direttamente
 * all'interno di SecurityConfiguration.java secondo la struttura standard dei progetti SIW.
 */
abstract class PasswordEncoderConfig {
    // Riservato per convenzioni SIW; configurazione attiva in SecurityConfiguration.java
}
